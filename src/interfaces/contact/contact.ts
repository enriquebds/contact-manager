export interface IContactRequest {
  id: string;
  name: string;
  email: string;
  telephone: string;
}

export interface IUpdateContactRequest {
  id: string;
  name: string;
  email: string;
  telephone: string;
}
